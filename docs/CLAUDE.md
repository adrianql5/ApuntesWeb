# docs/ — Salida generada

Carpeta 100 % generada por `node backend/src/index.js build`.

- NO editar nada aquí: cualquier cambio manual se pierde en el siguiente build.
- Para cambiar el sitio: edita `frontend/` (aspecto) o `backend/` (procesado) y
  reconstruye.
- Este archivo es el único que el build preserva.
